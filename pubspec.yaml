name: wappa_app
description: Una app de escritura colaborativa con IA (Flutter + Firebase)

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1
  firebase_messaging: ^16.0.1
  firebase_app_check: ^0.4.0+1
  image_picker: ^1.1.2

  # State management (BLoC)
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  google_sign_in: ^5.4.4

  google_fonts: ^6.2.1

  # Dependency Injection
  get_it: ^8.2.0

  # Editor de texto rico (para escribir capítulos/libros)
  flutter_quill: ^11.4.2

  # Preferencias locales (ej. cache de borradores)
  shared_preferences: ^2.2.3
  image: ^4.1.7

  # Internacionalización (fechas, números)
  intl: ^0.20.2
  share_plus: ^10.0.2

  # Íconos
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # 👇 Añadir aquí flutter_launcher_icons
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png

# 👇 Configuración de los íconos
flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"
  min_sdk_android: 21

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo.png"
  min_sdk_android: 21 # mínimo SDK para íconos adaptativos